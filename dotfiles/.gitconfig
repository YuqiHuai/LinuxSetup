[user]
	email = yhuai@uci.edu
	name = Yuqi Huai
[init]
	defaultBranch = main
[core]
	editor = nvim
	pager = cat
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
